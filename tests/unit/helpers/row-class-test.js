import { rowClass } from 'loopylog/helpers/row-class';
import { module, test } from 'qunit';

module('Unit | Helper | row class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rowClass([42]);
  assert.ok(result);
});
